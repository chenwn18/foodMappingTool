export function addMapping(generalID, standardID, standardAttributeIDs) {
    console.log('addMapping: ' + generalID + ' ' + standardID + ' ' + standardAttributeIDs);
}

export function deleteMapping(generalID, standardID, field) {
    console.log('deleteMapping: ' + generalID + ' ' + standardID + ' ' + field);
}

